/**
 * @param {number} x
 * @return {number}
 */
var reverse = function(x) {
    var result = Math.abs(x).toString().split('').reverse().join('');
    return result > 0x7FFFFFFF ? 0 : x < 0 ? result * -1 : result; 
};