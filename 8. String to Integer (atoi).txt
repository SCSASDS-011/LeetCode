/**
 * @param {string} s
 * @return {number}
 */
var myAtoi = function(s) {
    var num = parseInt(s.trim());
    return isNaN(num)? 0 : num >= Math.pow(2, 31) - 1 ? Math.pow(2, 31) - 1 : num <= (-1*Math.pow(2, 31)) ? -1 * Math.pow(2, 31) : num
};